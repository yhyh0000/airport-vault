name: 机场钥仓 Android Beta

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Beta tag, for example 2025.07.02-beta"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-beta:
    name: Build Android arm64-v8a and x86_64 Beta APKs
    runs-on: ubuntu-latest
    env:
      GO_VERSION: "1.24.0"
      NDK_VERSION: "r28c"
      CORE_CACHE_VERSION: "v1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Ensure Mihomo source is available
        shell: bash
        run: |
          if [ ! -d core/Clash.Meta/.git ]; then
            rm -rf core/Clash.Meta
            git clone --branch FlClash --depth 1 \
              https://github.com/songzhengpei/Clash.Meta.git \
              core/Clash.Meta
          fi

      - name: Record submodule commit
        id: submodule
        shell: bash
        run: echo "sha=$(git -C core/Clash.Meta rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Verify updater relationship contract
        shell: bash
        run: bash scripts/test_mihomo_core_relationship_contract.sh

      - name: Fetch submodule tags
        shell: bash
        run: |
          git -C core/Clash.Meta remote add metacubex https://github.com/MetaCubeX/mihomo.git 2>/dev/null || \
            git -C core/Clash.Meta remote set-url metacubex https://github.com/MetaCubeX/mihomo.git
          git -C core/Clash.Meta fetch --tags --force origin 2>/dev/null || true
          git -C core/Clash.Meta fetch --tags --force metacubex 2>/dev/null || true

      - name: Resolve beta tag
        id: vars
        shell: bash
        run: |
          TAG_NAME="${{ inputs.tag }}"

          if [ -z "$TAG_NAME" ]; then
            echo "TAG_NAME is empty"
            exit 1
          fi

          if ! [[ "$TAG_NAME" =~ ^[0-9]{4}\.[0-9]{2}\.[0-9]{2}-beta$ ]]; then
            echo "Tag must look like 2025.07.02-beta"
            exit 1
          fi

          # Beta builds use a sentinel version so they never prompt for a
          # stable GitHub release as an update. Stable builds still derive
          # their version from the release tag in the release workflow.
          VERSION_NAME="9.9.9"
          BUILD_NUMBER="$(date -u +%s)"

          echo "tag_name=$TAG_NAME" >> "$GITHUB_OUTPUT"
          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "build_number=$BUILD_NUMBER" >> "$GITHUB_OUTPUT"
          echo "apk_name=AirportVault-${TAG_NAME}-arm64-v8a.apk" >> "$GITHUB_OUTPUT"
          echo "apk_name_x64=AirportVault-${TAG_NAME}-x86_64.apk" >> "$GITHUB_OUTPUT"

      - name: Create tag
        shell: bash
        run: |
          TAG_NAME="${{ steps.vars.outputs.tag_name }}"

          git fetch --tags

          if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
            echo "Tag $TAG_NAME already exists locally."
          elif git ls-remote --tags origin "refs/tags/$TAG_NAME" | grep "$TAG_NAME" >/dev/null 2>&1; then
            echo "Tag $TAG_NAME already exists on origin."
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "$TAG_NAME" "$GITHUB_SHA"
            git push origin "$TAG_NAME"
          fi

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.0"
          cache-dependency-path: |
            core/go.sum

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.41.9
          cache: true

      - name: Show Flutter version
        run: flutter --version

      - name: Setup NDK
        id: setup_ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r28c
          add-to-path: true
          link-to-sdk: true

      - name: Resolve Go core cache key parts
        id: gocore
        shell: bash
        run: |
          echo "mihomo_sha=$(git -C core/Clash.Meta rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          git ls-files -z core plugins/setup/buildkit ':!core/Clash.Meta' \
            | xargs -0 sha256sum \
            | sha256sum \
            | awk '{print "local_sha="$1}' >> "$GITHUB_OUTPUT"

      - name: Cache Go core artifact
        id: cache-gocore
        uses: actions/cache@v4
        with:
          path: |
            libclash/android/arm64-v8a/libclash.so
            libclash/android/includes/arm64-v8a
            android/core/src/main/jniLibs/arm64-v8a/libclash.so
            android/core/src/main/cpp/includes/arm64-v8a
            libclash/android/x86_64/libclash.so
            libclash/android/includes/x86_64
            android/core/src/main/jniLibs/x86_64/libclash.so
            android/core/src/main/cpp/includes/x86_64
          key: gocore-${{ env.CORE_CACHE_VERSION }}-${{ runner.os }}-go${{ env.GO_VERSION }}-ndk${{ env.NDK_VERSION }}-${{ steps.gocore.outputs.local_sha }}-${{ steps.gocore.outputs.mihomo_sha }}

      - name: Prepare Android signing
        id: signing
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.KEYSTORE }}
          ANDROID_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          ANDROID_STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          ANDROID_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ -z "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "KEYSTORE secret is missing; using the Android debug key for a test build."
            echo "mode=debug" >> "$GITHUB_OUTPUT"
            cat > android/local.properties <<EOF
            sdk.dir=$ANDROID_HOME
            EOF
            sed -i 's/^[[:space:]]*//' android/local.properties
            exit 0
          fi

          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/app/keystore.jks

          cat > android/local.properties <<EOF
          sdk.dir=$ANDROID_HOME
          storeFile=keystore.jks
          storePassword=$ANDROID_STORE_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          keyPassword=$ANDROID_KEY_PASSWORD
          EOF
          sed -i 's/^[[:space:]]*//' android/local.properties

          echo "mode=release" >> "$GITHUB_OUTPUT"
          echo "Android signing files prepared."

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        # Keep the separately licensed Yucon reference app outside this
        # project's analyzer scope; its tests target Yucon's own package.
        run: flutter analyze --no-fatal-infos lib test

      - name: Enforce typography source contract
        run: flutter test test/theme/typography/typography_source_contract_test.dart

      - name: Verify Mihomo runtime config regression baseline
        run: |
          flutter test test/mihomo/runtime_config_regression_test.dart
          flutter test test/mihomo/source_config_preservation_test.dart
          flutter test test/mihomo/runtime_config_ownership_test.dart
          flutter test test/models/profile_atomic_write_test.dart
          flutter test test/core/controller_test.dart

      - name: Verify Phase 2CD compatibility suite
        run: |
          flutter test test/mihomo/structural_config_diff_test.dart
          flutter test test/mihomo/script_source_preservation_test.dart
          flutter test test/mihomo/e2e_compatibility_test.dart

      - name: Verify Go modules are tidy
        shell: bash
        run: |
          cd core
          go mod tidy
          if git diff --quiet go.mod go.sum; then
            echo "Go module files are already tidy."
          else
            echo "Go module files were normalized for the checked-out Mihomo source; continuing with the normalized graph."
            git diff --stat -- go.mod go.sum
          fi

      # Host-safe bridge/config tests compile against the patched Mihomo tree.
      # MihomoPatcher is idempotent, so the later Build Go core patch step
      # simply no-ops.
      - name: Apply Mihomo patch
        shell: bash
        run: |
          set -euo pipefail
          (
            cd plugins/setup/buildkit/build_tool
            dart pub get
            dart run bin/build_tool.dart \
              --root-dir "$GITHUB_WORKSPACE" patch-mihomo
          )

      - name: Validate Mihomo compatibility fixtures
        shell: bash
        run: cd core && go test ./providerbridge ./configfixture

      - name: Build Go core
        if: steps.cache-gocore.outputs.cache-hit != 'true'
        shell: bash
        env:
          ANDROID_NDK: ${{ steps.setup_ndk.outputs.ndk-path }}
          APP_ENV: beta
          PROJECT_DIR: ${{ github.workspace }}
        run: |
          bash plugins/setup/buildkit/run_build_tool.sh android --arch arm64
          bash plugins/setup/buildkit/run_build_tool.sh android --arch amd64

      - name: Verify Go core artifact
        shell: bash
        run: |
          test -f libclash/android/arm64-v8a/libclash.so
          test -f android/core/src/main/jniLibs/arm64-v8a/libclash.so
          ls -lh libclash/android/arm64-v8a/libclash.so
          ls -lh android/core/src/main/jniLibs/arm64-v8a/libclash.so
          test -f libclash/android/x86_64/libclash.so
          test -f android/core/src/main/jniLibs/x86_64/libclash.so
          ls -lh libclash/android/x86_64/libclash.so
          ls -lh android/core/src/main/jniLibs/x86_64/libclash.so

      - name: Resolve core metadata
        id: core
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          CORE_LIB="libclash/android/arm64-v8a/libclash.so"

          if [ ! -f "$CORE_LIB" ]; then
            echo "Expected core library not found: $CORE_LIB"
            find libclash android/core/src/main/jniLibs -type f || true
            exit 1
          fi

          CORE_SHA256="$(sha256sum "$CORE_LIB" | awk '{print $1}')"
          MIHOMO_VERSION="$(git -C core/Clash.Meta describe --tags --abbrev=0 2>/dev/null || true)"
          MIHOMO_COMMIT="$(git -C core/Clash.Meta rev-parse --short=12 HEAD)"
          CORE_BUILD_TIME="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"

          if [ -z "$MIHOMO_VERSION" ]; then
            echo "Could not resolve Mihomo version from git tag."
            exit 1
          fi

          MIHOMO_TAG="$MIHOMO_VERSION"
          if ! [[ "$MIHOMO_TAG" =~ ^v ]]; then
            MIHOMO_TAG="v$MIHOMO_TAG"
          fi
          MIHOMO_RELEASE_DATE="$(gh api "repos/MetaCubeX/mihomo/releases/tags/$MIHOMO_TAG" --jq '.published_at' 2>/dev/null || true)"
          if [ -z "$MIHOMO_RELEASE_DATE" ]; then
            MIHOMO_RELEASE_DATE="$CORE_BUILD_TIME"
          fi

          echo "core_sha256=$CORE_SHA256" >> "$GITHUB_OUTPUT"
          echo "mihomo_version=$MIHOMO_VERSION" >> "$GITHUB_OUTPUT"
          echo "mihomo_commit=$MIHOMO_COMMIT" >> "$GITHUB_OUTPUT"
          echo "mihomo_release_date=$MIHOMO_RELEASE_DATE" >> "$GITHUB_OUTPUT"
          echo "core_build_time=$CORE_BUILD_TIME" >> "$GITHUB_OUTPUT"

          {
            echo "### Core metadata"
            echo "- Mihomo version: $MIHOMO_VERSION"
            echo "- Mihomo commit: $MIHOMO_COMMIT"
            echo "- Mihomo release date: $MIHOMO_RELEASE_DATE"
            echo "- Core SHA256: $CORE_SHA256"
            echo "- Build time: $CORE_BUILD_TIME"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Resolve release notes
        id: notes
        shell: bash
        run: |
          TAG_NAME="${{ steps.vars.outputs.tag_name }}"
          RELEASE_REASON="Android arm64-v8a and x86_64 beta builds."

          NOTES_FILE="$GITHUB_WORKSPACE/build/release-notes.md"
          mkdir -p "$(dirname "$NOTES_FILE")"

          cat > "$NOTES_FILE" <<EOF
          $RELEASE_REASON

          Mihomo core: ${{ steps.core.outputs.mihomo_version }} (${{ steps.core.outputs.mihomo_commit }})
          Mihomo release date: ${{ steps.core.outputs.mihomo_release_date }}
          Core build time: ${{ steps.core.outputs.core_build_time }}
          Core SHA256: ${{ steps.core.outputs.core_sha256 }}
          EOF

          sed -i 's/^[[:space:]]*//' "$NOTES_FILE"
          echo "file=$NOTES_FILE" >> "$GITHUB_OUTPUT"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/**/gradle-wrapper.properties', 'android/gradle/**/*.toml', 'android/gradle.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Build APK
        env:
          SLCLASH_SKIP_GO_CORE_BUILD: "true"
        run: |
          flutter build apk --release --split-per-abi --target-platform android-arm64,android-x64 \
            --android-project-arg=slclashSkipAbiFilters=true \
            --android-project-arg=slclashSkipGoCoreBuild=true \
            --build-name "${{ steps.vars.outputs.version_name }}" \
            --build-number "${{ steps.vars.outputs.build_number }}" \
            --dart-define=APP_ENV=beta \
            --dart-define=APP_REPOSITORY="${{ github.repository }}" \
            --dart-define=CORE_SHA256="${{ steps.core.outputs.core_sha256 }}" \
            --dart-define=MIHOMO_VERSION="${{ steps.core.outputs.mihomo_version }}" \
            --dart-define=MIHOMO_COMMIT="${{ steps.core.outputs.mihomo_commit }}" \
            --dart-define=MIHOMO_RELEASE_DATE="${{ steps.core.outputs.mihomo_release_date }}" \
            --dart-define=CORE_BUILD_TIME="${{ steps.core.outputs.core_build_time }}"

      - name: Rename APKs
        shell: bash
        run: |
          set -euo pipefail
          declare -A APKS=(
            ["app-arm64-v8a-release.apk"]="${{ steps.vars.outputs.apk_name }}"
            ["app-x86_64-release.apk"]="${{ steps.vars.outputs.apk_name_x64 }}"
          )
          for source in "${!APKS[@]}"; do
            src="build/app/outputs/flutter-apk/$source"
            dest="${APKS[$source]}"
            if [ ! -f "$src" ]; then
              echo "Expected APK not found: $src"
              find build/app/outputs -type f || true
              exit 1
            fi
            cp "$src" "$dest"
            ls -lh "$dest"
          done

      - name: Verify APK manifest
        shell: bash
        run: |
          set -euo pipefail
          echo "=== APK Manifest Verification ==="

          # Locate aapt2 from Android SDK build-tools
          AAPT2="$(ls "$ANDROID_HOME"/build-tools/*/aapt2 2>/dev/null | head -1)"
          if [ -z "$AAPT2" ]; then
            echo "Error: aapt2 not found in Android SDK"
            exit 1
          fi

          EXPECTED_PACKAGE="com.slclash.app"
          if [ "${{ steps.signing.outputs.mode }}" = "debug" ]; then
            EXPECTED_PACKAGE="com.slclash.app.dev"
          fi
          PUBSPEC_BUILD="$(grep '^version: ' pubspec.yaml | sed 's/.*+//')"
          for APK in "${{ steps.vars.outputs.apk_name }}" "${{ steps.vars.outputs.apk_name_x64 }}"; do
            echo "--- $APK ---"
            PACKAGE="$($AAPT2 dump packagename "$APK")"
            echo "packageName: $PACKAGE"
            if [ "$PACKAGE" != "$EXPECTED_PACKAGE" ]; then
              echo "FAIL: packageName mismatch (expected $EXPECTED_PACKAGE)"
              exit 1
            fi
            VERSION_CODE="$($AAPT2 dump badging "$APK" 2>/dev/null | grep -o "versionCode='[0-9]*'" | cut -d"'" -f2)"
            echo "versionCode: $VERSION_CODE (pubspec build: $PUBSPEC_BUILD)"
            if [ -z "$VERSION_CODE" ] || [ "$VERSION_CODE" -le "$PUBSPEC_BUILD" ]; then
              echo "FAIL: versionCode is not newer than pubspec build number"
              exit 1
            fi
            jarsigner -verify -certs "$APK" 2>&1 | head -5
          done
          echo "PASS: both APKs passed manifest and signature checks"

          echo "=== All manifest checks passed ==="


      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: AirportVault-${{ steps.vars.outputs.tag_name }}-android-apks
          path: |
            ${{ steps.vars.outputs.apk_name }}
            ${{ steps.vars.outputs.apk_name_x64 }}

      - name: Publish GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG_NAME="${{ steps.vars.outputs.tag_name }}"
          APK_NAME_ARM64="${{ steps.vars.outputs.apk_name }}"
          APK_NAME_X64="${{ steps.vars.outputs.apk_name_x64 }}"

          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            echo "Release $TAG_NAME already exists. Refusing to overwrite existing release."
            exit 1
          fi

          echo "Creating pre-release $TAG_NAME..."
          gh release create "$TAG_NAME" "$APK_NAME_ARM64" "$APK_NAME_X64" \
            --title "机场钥仓 ${{ steps.vars.outputs.version_name }} Beta" \
            --notes-file "${{ steps.notes.outputs.file }}" \
            --prerelease
